defaults:
  - scope:
      path: ""
      type: "posts"
    values:
      layout: "post"
  - scope:
      path: ""
    values:
      layout: "default"

baseurl: /rearr

plugins:
  - jekyll-feed
  - jekyll-sitemap
  - jekyll-seo-tag
